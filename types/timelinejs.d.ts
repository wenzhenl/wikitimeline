declare module '@knight-lab/timelinejs' {
    // Add specific types for Timeline if available, otherwise use 'any'
    export class Timeline {
      constructor(element: HTMLElement, data: any, options?: any);
    }
  }